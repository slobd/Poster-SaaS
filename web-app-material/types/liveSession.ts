export interface ILiveSession {
    id: number
    avatar: {
        location: string
    }
    userId: number
    firstName: string
    lastName: string
    organizationName: string
    title: string
    link: string
    slug
}
