/**
 * @deprecated the setBanner and setBannerWithTimeout function have type definitions, so we don't
 * need to import thing Enum everywhere
 */
export enum FloatingBannerEnum {
    error = 'error',
    default = 'default',
    success = 'success',
}
